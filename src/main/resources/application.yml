spring:
    application:
          name: lyhm-ai-code-platform
server:
  port: 8123
  servlet:
    context-path: /api
# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.lyhm.lyhmaicodeplatform.controller

knife4j:
  enable: true
  setting:
    language: zh_cn
